# AI 模型代理服务 - 使用说明

## 📖 项目简介

AI 模型代理服务是一个基于 Node.js + TypeScript 的后端系统，作为前端客户端和 Tripo AI 3D 模型生成服务之间的代理层，集成了腾讯云 COS 云存储功能。

### 核心功能

- 🎯 **多输入支持**：支持文本描述或图片输入生成 3D 模型
- 🔄 **异步处理**：基于作业队列的异步处理机制
- ☁️ **自动存储**：生成的模型自动上传到腾讯云 COS
- 📊 **状态监控**：实时作业状态监控和轮询
- 🛡️ **安全可靠**：完整的错误处理、重试机制和安全中间件
- 📝 **标准化 API**：RESTful API 设计，支持 OpenAPI/Swagger 文档
- 🐳 **容器化部署**：完整的 Docker 和 Docker Compose 支持
- 🔍 **健康检查**：多层次健康检查，支持 Kubernetes 探针

### 支持的模型格式

| 格式 | 说明 | 支持状态 |
|------|------|---------|
| **GLB** | GLTF 二进制格式，推荐使用 | ✅ 支持 |
| **GLTF** | GL Transmission Format | ✅ 支持 |
| **OBJ** | Wavefront OBJ 格式 | ✅ 支持 |
| **FBX** | Autodesk FBX 格式 | ✅ 支持 |

## 🚀 快速开始

### 环境要求

- **Node.js**: 18.0.0 或更高版本
- **Redis**: 用于缓存和会话管理
- **腾讯云 COS**: 用于存储生成的模型文件
- **Tripo AI Token**: 用于调用 Tripo AI API

### 方式一：使用 Docker Compose（推荐）

这是最简单的启动方式，会自动启动应用和 Redis。

```bash
# 1. 克隆项目
git clone <repository-url>
cd ai-model-proxy

# 2. 复制环境变量文件
cp .env.example .env

# 3. 编辑 .env 文件，填入你的配置
nano .env

# 4. 启动服务
docker-compose up -d

# 5. 查看日志
docker-compose logs -f

# 6. 停止服务
docker-compose down
```

### 方式二：本地开发模式

```bash
# 1. 安装依赖
npm install

# 2. 启动 Redis（使用 Docker）
docker run -d -p 6380:6379 --name redis-local redis:7-alpine

# 3. 复制并配置环境变量
cp .env.local .env
nano .env

# 4. 启动开发服务器（支持热重载）
npm run dev
```

### 方式三：生产环境部署

```bash
# 1. 构建项目
npm run build

# 2. 启动生产服务
npm start
```

## ⚙️ 环境配置

### 必需配置项

编辑 `.env` 文件，配置以下必需项：

```bash
# 服务器配置
PORT=3000
HOST=0.0.0.0
NODE_ENV=production

# Tripo AI 配置
TRIPO_API_URL=https://api.tripo3d.ai
TRIPO_API_TIMEOUT=30000

# 腾讯云 COS 配置（必需）
TENCENT_COS_SECRET_ID=your_secret_id
TENCENT_COS_SECRET_KEY=your_secret_key
TENCENT_COS_REGION=ap-nanjing
TENCENT_COS_BUCKET=your_bucket_name

# Redis 配置
REDIS_HOST=localhost
REDIS_PORT=6380
REDIS_PASSWORD=
REDIS_DB=0
```

### 可选配置项

```bash
# 文件上传配置
MAX_FILE_SIZE=10485760  # 10MB
ALLOWED_IMAGE_TYPES=image/jpeg,image/png,image/webp

# 日志配置
LOG_LEVEL=info
LOG_FORMAT=json

# 安全配置
CORS_ORIGIN=*
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
```

## 📡 API 使用指南

### 访问 API 文档

服务启动后，访问以下地址查看完整的 API 文档：

- **Swagger UI**: http://localhost:3000/api-docs
- **OpenAPI JSON**: http://localhost:3000/api-docs.json

### 健康检查接口

```bash
# 基本健康检查
curl http://localhost:3000/health

# 就绪检查（检查依赖服务）
curl http://localhost:3000/api/v1/health/ready

# 详细健康检查
curl http://localhost:3000/api/v1/health/detailed

# 系统信息
curl http://localhost:3000/api/v1/health/info

# Kubernetes 存活探针
curl http://localhost:3000/api/v1/health/live

# Kubernetes 启动探针
curl http://localhost:3000/api/v1/health/startup
```

### 创建模型生成任务

#### 1. 文本生成 3D 模型（GLB 格式）

```bash
curl -X POST http://localhost:3000/api/v1/models \
  -F "type=text" \
  -F "input=a cute robot toy sitting on a table" \
  -F "token=YOUR_TRIPO_AI_TOKEN" \
  -F 'options={"format":"glb","quality":"medium"}'
```

**响应示例**：
```json
{
  "jobId": "f0258b02-978b-426e-b1bb-3f836fc37c2a",
  "status": "pending",
  "message": "模型生成请求已提交",
  "estimatedTime": 300
}
```

#### 2. 图片生成 3D 模型

```bash
curl -X POST http://localhost:3000/api/v1/models \
  -F "type=image" \
  -F "image=@/path/to/your/image.jpg" \
  -F "token=YOUR_TRIPO_AI_TOKEN" \
  -F 'options={"format":"glb","quality":"high"}'
```

#### 3. 参数说明

| 参数 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `type` | string | ✅ | 输入类型：`text` 或 `image` |
| `input` | string | ✅* | 文本描述（当 type=text 时必需） |
| `image` | file | ✅* | 图片文件（当 type=image 时必需） |
| `token` | string | ✅ | Tripo AI API Token |
| `options` | JSON | ❌ | 可选配置 |

**options 参数**：
```json
{
  "format": "glb",      // 模型格式：glb, gltf, obj, fbx
  "quality": "medium",  // 质量：low, medium, high
  "timeout": 300000     // 超时时间（毫秒）
}
```

### 查询任务状态

```bash
curl http://localhost:3000/api/v1/models/{jobId}/status
```

**响应示例**：
```json
{
  "jobId": "f0258b02-978b-426e-b1bb-3f836fc37c2a",
  "status": "processing",
  "message": "正在生成模型",
  "progress": 45
}
```

**状态说明**：
- `pending`: 等待处理
- `processing`: 正在生成
- `completed`: 生成完成
- `failed`: 生成失败

### 获取模型结果

```bash
curl http://localhost:3000/api/v1/models/{jobId}/result
```

**响应示例**：
```json
{
  "jobId": "f0258b02-978b-426e-b1bb-3f836fc37c2a",
  "status": "completed",
  "message": "模型生成已完成",
  "result": {
    "modelUrl": "https://your-bucket.cos.ap-nanjing.myqcloud.com/model_xxx.glb",
    "metadata": {
      "fileSize": 1024000,
      "format": "glb",
      "generationTime": 98000
    }
  }
}
```

### 调试接口

```bash
# 获取所有作业状态
curl http://localhost:3000/api/v1/models/debug/jobs
```

## 🧪 测试

### 运行测试

```bash
# 运行所有测试
npm test

# 监视模式（开发时使用）
npm run test:watch

# 生成覆盖率报告
npm run test:coverage
```

### API 接口测试

项目提供了完整的 API 测试脚本：

```bash
# 测试所有接口（包括 GLB 格式）
./test-all-apis-glb.sh

# 简化的模型 API 测试
./test-model-api.sh
```

测试报告会自动生成在项目根目录：
- `API_TEST_REPORT_GLB_*.md`
- `API_TEST_FINAL_REPORT.md`

## 🔧 开发指南

### 项目结构

```
ai-model-proxy/
├── src/
│   ├── config/          # 配置管理
│   ├── controllers/     # 控制器层（处理 HTTP 请求）
│   ├── services/        # 业务逻辑层
│   ├── clients/         # 外部服务客户端（Tripo AI, COS）
│   ├── middleware/      # 中间件（验证、错误处理等）
│   ├── routes/          # 路由定义
│   ├── types/           # TypeScript 类型定义
│   ├── utils/           # 工具函数
│   ├── docs/            # OpenAPI 文档
│   └── test/            # 测试辅助工具
├── logs/                # 日志文件
├── docs/                # 项目文档
├── .env                 # 环境变量配置
├── docker-compose.yml   # Docker Compose 配置
└── package.json         # 项目依赖
```

### 可用的 npm 命令

```bash
# 开发
npm run dev              # 启动开发服务器（热重载）
npm run build            # 构建生产版本
npm start                # 运行生产版本

# 测试
npm test                 # 运行测试
npm run test:watch       # 监视模式
npm run test:coverage    # 生成覆盖率报告

# 代码质量
npm run lint             # 检查代码规范
npm run lint:fix         # 自动修复代码问题
npm run format           # 格式化代码
npm run type-check       # TypeScript 类型检查

# Docker
npm run docker:build     # 构建 Docker 镜像
npm run docker:run       # 运行 Docker 容器
npm run docker:dev       # 启动开发环境
npm run docker:prod      # 启动生产环境
```

### 代码规范

项目使用 ESLint 和 Prettier 进行代码规范检查：

```bash
# 检查代码规范
npm run lint

# 自动修复问题
npm run lint:fix

# 格式化所有代码
npm run format
```

## 🐳 Docker 部署

### 使用 Docker Compose

```bash
# 启动所有服务
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down

# 重新构建并启动
docker-compose up --build -d
```

### 单独使用 Docker

```bash
# 构建镜像
docker build -t ai-model-proxy .

# 运行容器
docker run -d \
  -p 3000:3000 \
  --env-file .env \
  --name ai-model-proxy \
  ai-model-proxy

# 查看日志
docker logs -f ai-model-proxy

# 停止容器
docker stop ai-model-proxy
```

## 📊 监控和日志

### 日志文件

日志文件存储在 `logs/` 目录：

```bash
# 查看综合日志
tail -f logs/combined.log

# 查看错误日志
tail -f logs/error.log
```

### 日志级别

可以通过 `LOG_LEVEL` 环境变量设置日志级别：

- `debug`: 详细的调试信息
- `info`: 一般信息（默认）
- `warn`: 警告信息
- `error`: 错误信息

### 健康检查

```bash
# 检查服务是否健康
curl http://localhost:3000/health

# 检查所有依赖服务
curl http://localhost:3000/api/v1/health/detailed
```

## 🔐 安全配置

### CORS 配置

在 `.env` 文件中配置允许的来源：

```bash
# 允许所有来源（开发环境）
CORS_ORIGIN=*

# 允许特定来源（生产环境）
CORS_ORIGIN=https://your-frontend-domain.com
```

### 速率限制

默认配置：
- 全局限制：15 分钟内最多 100 个请求
- API 限制：15 分钟内最多 50 个请求

可以通过环境变量调整：

```bash
RATE_LIMIT_WINDOW_MS=900000      # 时间窗口（毫秒）
RATE_LIMIT_MAX_REQUESTS=100      # 最大请求数
```

### 文件上传限制

```bash
MAX_FILE_SIZE=10485760           # 最大文件大小（字节）
ALLOWED_IMAGE_TYPES=image/jpeg,image/png,image/webp
```

## 🔑 获取 Tripo AI Token

1. 访问 [Tripo AI 平台](https://platform.tripo3d.ai)
2. 登录或注册账户
3. 进入 API Keys 或 Settings 页面
4. 生成新的 API Token
5. **注意**：Token 通常以 `tsk_` 开头

**重要提示**：
- Token 是敏感信息，不要提交到 Git
- 每次 API 调用都需要传递 Token
- Tripo AI 按使用量计费，注意控制成本

## 📚 相关文档

- [README.md](./README.md) - 项目概述
- [LOCAL_DEVELOPMENT.md](./LOCAL_DEVELOPMENT.md) - 本地开发指南
- [TRIPO_AI_INTEGRATION_GUIDE.md](./TRIPO_AI_INTEGRATION_GUIDE.md) - Tripo AI 集成指南
- [API_TEST_FINAL_REPORT.md](./API_TEST_FINAL_REPORT.md) - API 测试报告
- [TEST_COVERAGE_IMPROVEMENTS.md](./TEST_COVERAGE_IMPROVEMENTS.md) - 测试覆盖率报告

## ❓ 常见问题

### 1. Redis 连接失败

**问题**：应用无法连接到 Redis

**解决方案**：
```bash
# 检查 Redis 是否运行
docker ps | grep redis

# 启动 Redis
docker run -d -p 6380:6379 --name redis-local redis:7-alpine

# 测试连接
redis-cli -h localhost -p 6380 ping
```

### 2. Tripo AI 认证失败

**问题**：Token 认证失败

**解决方案**：
- 确认 Token 格式正确（以 `tsk_` 开头）
- 检查账户是否有足够余额
- 确认 Token 有 API 访问权限
- 重新生成新的 Token

### 3. 端口已被占用

**问题**：端口 3000 或 6380 已被占用

**解决方案**：
```bash
# 修改 .env 文件中的端口
PORT=3001
REDIS_PORT=6381
```

### 4. 文件上传失败

**问题**：图片上传失败或文件过大

**解决方案**：
- 检查文件大小是否超过限制（默认 10MB）
- 确认文件格式是否支持（JPEG, PNG, WebP）
- 调整 `MAX_FILE_SIZE` 环境变量

### 5. 模型生成超时

**问题**：模型生成时间过长

**解决方案**：
- 3D 模型生成通常需要 2-5 分钟
- 可以通过轮询状态接口查看进度
- 调整 `options.timeout` 参数

## 🤝 技术支持

如果遇到问题：

1. 查看日志文件：`logs/error.log`
2. 检查健康检查接口：`/api/v1/health/detailed`
3. 查看 API 文档：`/api-docs`
4. 运行测试脚本：`./test-all-apis-glb.sh`

## 📄 许可证

MIT License

---

**最后更新**：2026年1月15日
