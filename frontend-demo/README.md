# AI 3D 模型生成器 - Vue 3 前端示例

这是一个使用 Vue 3 + Vite 构建的前端示例，用于演示如何调用 AI Model Proxy 后端服务生成 3D 模型。

## ✨ 功能特性

- 🎨 **文本到 3D 模型** - 输入文字描述生成 3D 模型
- 🖼️ **图片到 3D 模型** - 上传图片生成 3D 模型
- 📊 **实时进度显示** - 显示生成进度和状态
- 🔍 **服务健康检查** - 检查后端服务状态
- 📱 **响应式设计** - 支持移动端和桌面端
- ⚡ **现代化 UI** - 使用渐变色和动画效果

## 🚀 快速开始

### 1. 安装依赖

```bash
cd frontend-demo
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

### 3. 访问应用

打开浏览器访问: http://localhost:5173

## 🔧 配置说明

### 代理配置

前端通过 Vite 代理转发请求到后端服务：

```javascript
// vite.config.js
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:3000',  // 后端服务地址
      changeOrigin: true,
    }
  }
}
```

### API Token

使用前需要在界面中输入有效的 Tripo AI Token。

## 📁 项目结构

```
frontend-demo/
├── src/
│   ├── composables/
│   │   └── useModelGeneration.js  # 模型生成逻辑
│   ├── App.vue                    # 主应用组件
│   ├── main.js                    # 应用入口
│   └── style.css                  # 全局样式
├── index.html                     # HTML 模板
├── package.json                   # 项目配置
├── vite.config.js                 # Vite 配置
└── README.md                      # 说明文档
```

## 🎯 使用方法

### 文本到 3D 模型

1. 输入 Tripo AI Token
2. 在文本框中描述想要的 3D 模型
3. 选择生成质量
4. 点击"生成 3D 模型"按钮
5. 等待生成完成并下载模型

### 图片到 3D 模型

1. 输入 Tripo AI Token
2. 上传图片文件 (支持 JPEG, PNG, WebP)
3. 选择生成质量
4. 点击"生成 3D 模型"按钮
5. 等待生成完成并下载模型

## 🔍 技术栈

- **Vue 3** - 渐进式 JavaScript 框架
- **Vite** - 快速的前端构建工具
- **Axios** - HTTP 客户端
- **Composition API** - Vue 3 组合式 API
- **CSS3** - 现代化样式和动画

## 📝 API 调用示例

```javascript
// 创建文本到3D模型任务
const formData = new FormData()
formData.append('type', 'text')
formData.append('input', 'a cute red apple')
formData.append('token', 'your-token')
formData.append('options[quality]', 'high')

const response = await axios.post('/api/v1/models', formData)
```

## 🛠️ 开发命令

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览生产版本
npm run preview
```

## 🔗 相关链接

- [后端 API 文档](../FRONTEND_API_GUIDE.md)
- [Vue 3 官方文档](https://vuejs.org/)
- [Vite 官方文档](https://vitejs.dev/)

## 📄 许可证

MIT License