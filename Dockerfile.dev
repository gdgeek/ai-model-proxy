# 开发环境Dockerfile
FROM node:22-alpine

# 安装开发工具
RUN apk add --no-cache git

# 创建应用目录
WORKDIR /app

# 复制package文件
COPY package*.json ./

# 安装所有依赖（包括开发依赖）
RUN npm ci --silent

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 3000 9229

# 启动开发服务器
CMD ["npm", "run", "dev"]